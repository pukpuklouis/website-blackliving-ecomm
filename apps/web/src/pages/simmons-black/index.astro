---
import CategoryPageLayout from '../../components/CategoryPageLayout.astro';
import { getEntry } from 'astro:content';

const categoryEntry = await getEntry('categories', 'simmons-black');

if (!categoryEntry) {
  return Astro.redirect('/404');
}

const categoryConfig = categoryEntry.data;
---

<CategoryPageLayout {categoryConfig} />
