---
import AccountNavComponent from './ui/AccountNav.tsx';

export interface Props {
  currentPage: 'profile' | 'orders' | 'appointments';
}

const { currentPage } = Astro.props;
---

<AccountNavComponent {currentPage} client:only="react" />
